/**
 * src/lib/telegram-store.ts  — DEAD FILE, safe to delete
 *
 * This shim was a compatibility layer while migrating from the old in-memory
 * store to SQLite. All callers have been updated to import from telegram-db.ts
 * directly. No file in the codebase imports this module any more.
 *
 * Run the following to remove it:
 *   rm src/lib/telegram-store.ts
 */
